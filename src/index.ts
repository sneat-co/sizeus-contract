// @sneat/sizeus-contract — frozen cross-repo contract surface for the sizeus extension.
//
// Shared DTOs (size records, catalog models, …) will be exported from here so
// that both the Sneat super-app extension libs and the standalone sizechart.app
// resolve every shared model from this single package.
//
// Intentionally empty for now: the DTOs land in a follow-up task.

export {};
